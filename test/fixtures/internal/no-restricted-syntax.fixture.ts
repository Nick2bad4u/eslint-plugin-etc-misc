declare const condition: boolean;
declare const value: number;

if (condition) {
    console.log("if");
}

while (condition) {
    break;
}

switch (value) {
    case 1:
        break;
    default:
        break;
}

for (;;) {
    break;
}

const result = condition ? "yes" : "no";

void result;
