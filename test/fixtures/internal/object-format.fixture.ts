const singleLineTooWide = { a: 1, b: 2 };
const anotherSingleLineTooWide = { x: 1, y: 2 };

const validMultiline = {
    a: 1,
    b: 2,
};

const shouldBeSingleLine = {
    a: 1,
    b: 2,
};

const singleProperty = { a: 1 };

export {
    anotherSingleLineTooWide,
    shouldBeSingleLine,
    singleLineTooWide,
    singleProperty,
    validMultiline,
};
